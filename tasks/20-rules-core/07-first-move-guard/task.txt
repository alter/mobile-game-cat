TASK: A board with no first move ends, not hangs

GOAL
  Уровень, где все верхние предметы заперты, сегодня даёт мёртвый экран:
  IsOver=false, Outcome=null, взять нечего. Защита стоит после удавшегося
  взятия и потому не срабатывает никогда.

CONTEXT
  Core/Board.cs (:169-174 проверка в конце TakeItem), Core/Level.cs (:23-85 —
  замки не проверяются, LockedAfterTriples без верхней границы),
  Tests/Core/PartialInformationTests.cs (:119 — тест этого класса отказа,
  проходящий мимо первого хода). Аудит 2026-09-01, разбор Core, п. 2.1.

SCOPE
  + Board после постройки (и Level при чтении) распознаёт «ходов нет уже
    сейчас» и даёт исход, а не молчание.
  + LockedAfterTriples получает верхнюю границу от достижимого числа троек.
  + Тест: уровень из одних запертых — исход есть до первого взятия.
  - Отгружаемые 37 уровней не трогать: в них дефекта нет (проверено 01.09).

OUTCOME
  Собранный из запертых предметов уровень даёт Outcome без единого хода;
  тест это закрепляет.

VERIFY (QA)
  1. dotnet test: новый тест зелёный, 261 старых не тронуты.

ROLE
  CORE
