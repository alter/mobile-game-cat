TASK: Thirty clutter props

GOAL
  The pile the player digs through: 30 household items, one visual set, each
  distinguishable from the others at tile size.

CONTEXT
  art-brief.md section 6 (silhouette/colour families, readability
  constraint, acceptance)
  art-prompts.md section 3 (30-item table, prompt template, nuances: no
  food, no text, no reflections, wear-not-destruction)

SCOPE
  + 30 files, one generation batch, one prompt template, one seed/session.
  + Curation: background cleanup, alpha check, crop/centre to uniform frame.
  + One contact-sheet.png of all 30 for the coherence check.
  - Not prop_unknown or prop_locked - see 40-art/02-prop-blockers.
  - Not the reward items - see 40-art/08-rewards, deliberately "nicer" than
    this set and must not share its palette rule.

OUTCOME
  30 PNGs prop_<name>.png, 256x256, transparent alpha, uniform frame, under
  game/Assets/Resources/Art, plus contact-sheet.png under game/Assets/Art.

  Resources, not plain Assets: the view loads a prop by name at runtime
  (Resources.Load<Texture2D>($"Art/{kind}")), and only Resources can be read
  that way. The shipped levels do the same as the 37 level JSONs already in
  Resources/Levels. The contact sheet is a review artefact, never loaded by
  the game, so it stays out of Resources - anything in there ships in the
  build whether it is used or not.

VERIFY (HUMAN)
  1. All 30 files present, 256x256, clean alpha - checked by QA, not ART.
     Done 2026-08-27: 32 files (30 + the two from 40-art/02), all 256x256
     RGBA with real transparency.
  2. Contact sheet reviewed by someone other than the author: reads as one
     set, not a mismatched pile.
  3. Outsider shown any 10 of the 30 shrunk to 52px: confident yes that they
     are 10 different things, answer recorded in VERIFY.md.

ROLE
  ART, QA, HUMAN

DEPENDS
  00-validate-demand/03-art-prompt-props
