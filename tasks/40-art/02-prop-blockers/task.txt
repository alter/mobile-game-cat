TASK: Buried-item blank and lock overlay

GOAL
  Two assets the old list never named: the blank a buried item shows before
  it is reachable, and the overlay a locked item wears until cleared.

CONTEXT
  art-brief.md section 10, subsections "Blank tile — item under the clutter"
  and "Locked item"
  art-prompts.md section 6, same two subsections
  cat-shelter-tasks.md lines 476, 478 (3.9 hidden kinds, 3.11 complications -
  why these exist)

SCOPE
  + prop_unknown.png: "something is here", no reveal - draped-cloth
    silhouette, low detail (tiles the screen in numbers, must stay quiet).
  + prop_locked.png: semi-transparent overlay, prop underneath still shows.
  - Not the 30 base props under it - see 40-art/01-props.
  - No chain, padlock, metal, rust on the lock - reads as punishment, not
    care, per art-prompts.md section 6.
  - No skull, face or anything recognisable under the cloth.

  Note (2026-08-27): hiding buried kinds was measured to change win rate by
  0.0 +/- 1.2 percentage points, so prop_unknown exists for the feel of
  discovery rather than for difficulty - see 30-levels-solver/10. It is still
  needed while D3 stands; it disappears with D3 if D3 is ever reversed.

OUTCOME
  2 PNGs, prop_unknown.png and prop_locked.png, 256x256, same frame/alpha
  rules as the 30 props, under game/Assets/Resources/Art (see 40-art/01 for
  why Resources).

VERIFY (QA)
  1. Both files present, 256x256, clean alpha.
  2. prop_unknown in the 30-prop contact sheet: viewer confirms "something
     is under there" without naming an object.
  3. prop_locked composited over any one of the 30 props: prop underneath
     still identifiable through the overlay.
     Fails as an overlay, passes as a badge. Composited full-tile over five
     different props on 2026-08-27, all five became the same picture:
     prop_locked is a coil of rope drawn as its own object, 59% opaque, and
     its transparency is the background around the coil rather than gaps to
     see the item through. The view therefore draws it at 26px in the tile's
     lower-right corner, where it marks the prop instead of replacing it, and
     the prop stays fully readable at tile size.

     Redraw it as a true overlay - rope crossing the item, most of the frame
     open - and this check passes as written; the view goes back to full-tile
     by changing two numbers in .game__tile-lock. Not blocking: the rules now
     show the lock (D15, decided), so the mechanic is on screen either way.

ROLE
  ART, QA

DEPENDS
  40-art/01-props
