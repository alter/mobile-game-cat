TASK: TraitsResponse drops the spot, not the answer

GOAL
  Одна лишняя или незнакомая примета не должна стоить игроку кота с его
  фотографии. Сегодня стоит: весь ответ модели летит в мусор.

CONTEXT
  Core/TraitsResponse.cs (:51-57 catch → null; :61-68 String() протекает через
  null-значения), Core/CatTraits.cs (:80-86 броски; :186-191 записанный
  принцип «молчать про поле, а не про всё»), Core/CatSpot.cs (:46-47),
  Tests/Core/TraitsResponseTests.cs.

SCOPE
  + Лишние приметы отбрасываются по одной (первые две валидные остаются),
    незнакомое место отбрасывает примету, не ответ.
  + null в значении поля читается как отсутствующее поле, а не как имя
    следующего ключа.
  + Тесты: три приметы; повтор места; незнакомое место; base_color:null;
    white_markings:null со следующим spots.
  - Не менять схему Worker и не трогать сам CatTraits.Allowed.

OUTCOME
  Ответ модели с тремя приметами или с null-полем даёт кота с верными цветом,
  узором, шерстью и глазами; тесты это закрепляют.

VERIFY (QA)
  1. dotnet test: новые случаи зелёные, старые не тронуты.
  2. Ручной прогон JSON из SCOPE через TraitsResponse.Parse — не null.

ROLE
  CORE

DEPENDS
  50-photo/03-traits-schema
