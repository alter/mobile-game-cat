TASK: iOS catches up with the Android hardening

GOAL
  Android прошёл боевой разбор 01.09 (предел кадра с замером, коды ошибок,
  очистка файлов); iOS остался как написан. Четыре известных долга и один
  открытый вопрос с замером.

CONTEXT
  Plugins/iOS/CatVision.swift (:58 — предела стороны нет, :76 — проза
  системного языка через границу), CatMarks.swift (:426,:493 — полный кадр в
  память, ~49 МБ на снимке камеры; :615,:649,:700 — проза), CatPicker.swift
  (:54 — временная папка не чистится при гибели), CatShare.java (:104 —
  карточка лежит в кэше навсегда; это Android, но долг того же рода),
  Decode.java (:36-81 — как выглядит сделанная половина).

SCOPE
  + Замер на устройстве: зависит ли VNRecognizeAnimalsRequest от размера
    кадра, как ML Kit (таблица тех же семи размеров). Не гадать: если не
    зависит — предел не нужен, записать числа и не трогать.
  + Проза системного языка через границу → коды, как в CatPicker.swift:22-29.
  + Очистка временной папки снимка на старте выбора (как purge на Android).
  + Удаление kitten-card.png после отправки на Android (CatShare.java).
  + minSdkVersion 25 в CatShare.androidlib/build.gradle → 33, как в проекте.
  - Приметы (CatMarks) на Android не переносить — отдельное решение и вес.

OUTCOME
  Таблица «размер кадра → вердикт» для iOS лежит в NOTES.md с вердиктом про
  предел; строки ошибок через границу — только коды; файлы снимка и карточки
  не переживают своё назначение.

VERIFY (QA)
  1. Таблица с устройства, семь размеров, числа.
  2. grep по Swift: localizedDescription не пересекает границу.
  3. Прогон отправки на Android: файла в cache/share после отправки нет.

ROLE
  NATIVE

DEPENDS
  10-accounts/03-xcode
