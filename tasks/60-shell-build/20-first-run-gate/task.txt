TASK: First-run gate must not close itself

GOAL
  Экран съёмки — единственный путь к обещанию игры. Сегодня три дыры позволяют
  игре молча забрать его навсегда или молча сломать переход с него.

CONTEXT
  Shell/CatIdentity.cs (:52-60 тихая запись, :73 Forget без вызывающих),
  Shell/GameBoot.cs (:559-569 ветка board.txt выше HasACat, :668-671 ворота,
  :684 и :753 AddComponent без проверки повтора, :754 голый Build),
  View/CaptureScreen.cs (:842 SetBusy без проверки _busy),
  аудит 2026-09-01.

SCOPE
  + CatIdentity.Traits перестаёт писать cat.save как побочное действие чтения;
    запись — только явным действием (подтверждение имени).
  + Путь игрока к MeetYourCatScreen защищён так же, как отладочный: отказ
    Build виден игроку словами, экран съёмки возвращает кнопки.
  + Оба AddComponent получают проверку повторного добавления, как остальные
    шесть мест.
  + SetBusy переживает несобранный экран.
  + Тест или прогон, воспроизводящий сценарий владельца: выбрал кота, не
    оставил, закрыл, открыл, выбрал другого — экран съёмки на месте.
  - SafeBuild вокруг AddComponent не трогать здесь — что он не ловит OnEnable,
    известно и записано (DeviceLog.cs:29); отдельного лечения не требует.

OUTCOME
  Ни один путь в коде не создаёт cat.save без явного действия игрока, и отказ
  на пути к экрану знакомства оставляет игрока с работающими кнопками.

VERIFY (QA)
  1. grep: CatSaveFile.Write вызывается только из подтверждения имени.
  2. Прогон сценария владельца на эмуляторе, снимки экрана.
  3. Искусственный отказ Build (порча каркаса) — экран съёмки показывает
     сообщение и кнопки, а не вечную полосу.

ROLE
  VIEW

DEPENDS
  50-photo/09-meet-your-cat
