TASK: Subset fonts can never lose a glyph silently

GOAL
  25 знаков нового текста однажды стали бы пустыми квадратами — поймали
  руками (коммит 9478eb4). Аудит 28.08 прописал лекарство: утверждение с
  числом требует теста. Это тот тест.

CONTEXT
  game/Assets/Shell/Copy*.cs (все три), подрезанные NotoSans*-Regular.otf в
  game/Assets/..., tools/ (куда положить проверку), коммит 9478eb4.

SCOPE
  + Проверка в pytest: каждый символ каждой строки каждого языка есть в cmap
    соответствующего шрифта (fontTools уже в requirements).
  + Подключить в build/headless-build.sh той же строкой, что остальные.
  - Не раздувать подрезку «на вырост» — проверка и есть защита.

OUTCOME
  Новый текст с непокрытым знаком валит pytest до сборки, а не игрока после.

VERIFY (QA)
  1. Мутация: добавить в Copy строку с редким знаком → тест красный; убрать →
     зелёный.

ROLE
  TOOLS

DEPENDS
  60-shell-build/16-localisation-ready
