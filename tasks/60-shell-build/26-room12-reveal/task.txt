TASK: Room 12 gets its reveal before the ending card

GOAL
  Наблюдение владельца: последний угол комнаты 12 не открывается. Механизм
  найден (30-levels-solver/13): последняя куча комнаты 12 — последний уровень
  игры, и Finish() уходит в ShowEndingCard() с return раньше показа
  преображения комнаты. Прогресс засчитан, но открытие четвёртого угла и
  чистую комнату 12 игрок не видит никогда.

CONTEXT
  game/Assets/View/DebugGameView.cs: ветка _plan.Next(_level) == null (~1149),
  ShowRoomTransformation и lastPileOfRoom (~1193), RenderRoom (~531 —
  Mathf.Clamp(_level.PileIndex, 0, 4)). Разбор:
  tasks/30-levels-solver/13-shipped-levels-drift/NOTES.md.

SCOPE
  + Комната 12 показывает своё преображение (четвёртый угол, чистый фон),
    и только потом — финальная карточка игры.
  + Порядок и время показа — решить по духу остальных переходов (полоса,
    задержка 120 мс); финальная карточка не теряется и показывается один раз.
  - Саму финальную карточку (Review.cs, 60/11) не менять.
  - Сохранение в этой ветке не трогать — оно чинилось 29.08 и правильное.

OUTCOME
  Прохождение последней кучи комнаты 12 показывает преображение комнаты,
  затем финальную карточку; снимки экрана обоих в задаче.

VERIFY (QA)
  1. Прогон до конца комнаты 12 на эмуляторе (board.txt-сбруя или
     сохранение перед последней кучей), снимки: преображение → карточка.

ROLE
  VIEW

DEPENDS
  60-shell-build/11-post-level-12
