TASK: Guards for the 2026-09-02 fixes

GOAL
  Четыре исправления этого дня держатся на дисциплине, а не на проверке.
  Каждое откатывается одной строкой, и никто этого не заметит до жалобы
  владельца — ровно так, как описывает AUDIT-2026-08-28: «утверждение,
  верное при записи и никем не перечитанное».

CONTEXT
  tools/tests/test_analytics_call_sites.py (образец жанра: ищет места вызова
  по исходникам и падает, когда их число или расположение меняется),
  tasks/AUDIT-2026-08-28.md (правило, которое это применяет), коммиты
  22a073f (ворота первого запуска), efd3354 (главный поток), ddc64d7 (шуба
  по кадрам), и защита AddComponent в Shell/GameBoot.cs.

SCOPE
  + cat.save пишется ровно из одного места — подтверждения имени игроком.
  + Распознавание и чтение шубы не зовутся напрямую из тела метода: путь
    через Shell/OffMain.cs либо через пошаговый корутинный вариант.
  + CoatBuilder.TryBuild напрямую зовут только запекающие средства редактора,
    и те — после уменьшения, размером не больше 512.
  + Каждый AddComponent в GameBoot защищён проверкой на повтор; число мест
    закреплено, чтобы новое незащищённое падало.
  + У каждой проверки — мутационная проба в NOTES.md: дословный красный
    прогон, откат, дословный зелёный.
  - Не закреплять то, что нельзя проверить статически (например, что полоса
    ожидания действительно движется) — для этого есть прогоны на устройстве.

OUTCOME
  tools/tests/test_regression_guards.py в наборе pytest; откат любого из
  четырёх исправлений делает прогон красным.

VERIFY (QA)
  1. ./.venv/bin/python -m pytest tools/tests -q — зелёные.
  2. Мутация каждого из четырёх — красный прогон с именем упавшей проверки.

ROLE
  QA
