# Где лежит артефакт OUTCOME — 2026-09-01

OUTCOME просит таблицу по всем 41 снимку в ЭТОМ каталоге. Таблица выросла в
другом месте, и переносить её сюда значило бы завести вторую копию, которая
разойдётся с первой:

- числа ML Kit рядом с числами Vision — `tasks/50-photo/05-vision-plugin/NOTES-android.md`;
- прогон полного пути по 45 снимкам — `tasks/50-photo/06-photo-outcome/VERIFY-2026-09-01.md`.

Вердикт, одной строкой: ML Kit ЗАМЕНЯЕТ Vision для первой ступени — с двумя
оговорками, вписанными в код: кадр режется до 1280 по длинной стороне
(`Decode.java`, замер в комментарии), и целый кадр опрашивается всегда, а не
только при пустых вырезках (`CatVision.java`).
